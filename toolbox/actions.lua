Actions = {
  GAME_PREPARE = "GAME_PREPARE", -- called before dungeon generation
  GAME_START = "GAME_START", -- called when the match starts
  RESPAWN = "RESPAWN" -- called when knight is preparing to spawn. Gives a player as parameter of the function
}
